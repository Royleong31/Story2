<!DOCTYPE html>
<html lang="en" dir="ltr">

<head>
  <meta charset="utf-8">
  <title>Day of the week</title>
  <link rel="stylesheet" href="styles.css">
</head>
<%# scriplet tag allows you to do control flow, but need to use the angled brackets every time js is in html %>

<body>
  <div class="box" id='heading'>
    <h1> <%= kindOfDay %> Tasks</h1>
  </div>
  
  <div class="box">
    <% for (let i=0; i<newListItem.length; i++) { %>
      <div class="item">
        <input type='checkbox'>
        <p><%= newListItem[i] %></p>
      </div>
    <% } %>
    
    <form class='item' action="/" method="post">
      <input type="text" name="newTask" placeholder="New Task" autocomplete="off" autofocus>
      <button type="submit" name='list' value= <%= kindOfDay %> >+</button>
    </form>
  </div>
    
  

</body>

</html>